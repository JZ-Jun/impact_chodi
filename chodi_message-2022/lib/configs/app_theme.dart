import 'package:flutter/cupertino.dart';

class AppTheme {
  static const LinearGradient containerLinearGradient = LinearGradient(
      begin: Alignment.topCenter,
      end: Alignment.bottomCenter,
      colors: [Color(0xFF65E1F8), Color(0xFF97E3F1), Color(0xFFE1FAFE)]);
  static const Color fontColor = Color(0xFF7B7B7B);
  static const Color editTextColors = Color(0x8FFFFFFF);
}
